#!/usr/bin/make -f
# -*- makefile -*-

%:
	dh $@

override_dh_auto_configure:
	dh_auto_configure -- -DCMAKE_POLICY_VERSION_MINIMUM=3.5

override_dh_auto_install:
	dh_auto_install
	install -m755 $(CURDIR)/debian/tunics $(CURDIR)/debian/zelda-tunics/usr/bin/tunics